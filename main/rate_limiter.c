#include "rate_limiter.h"
#include "esp_log.h"

static const char *TAG = "RATE_LIMITER";

esp_err_t rate_limiter_init(void)
{
    ESP_LOGI(TAG, "Rate limiter initialized (placeholder)");
    return ESP_OK;
}