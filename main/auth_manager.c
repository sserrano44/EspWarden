#include "auth_manager.h"
#include "esp_log.h"

static const char *TAG = "AUTH_MANAGER";

esp_err_t auth_manager_init(void)
{
    ESP_LOGI(TAG, "Auth manager initialized (placeholder)");
    return ESP_OK;
}