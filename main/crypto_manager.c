#include "crypto_manager.h"
#include "esp_log.h"

static const char *TAG = "CRYPTO_MANAGER";

esp_err_t crypto_manager_init(void)
{
    ESP_LOGI(TAG, "Crypto manager initialized (placeholder)");
    return ESP_OK;
}