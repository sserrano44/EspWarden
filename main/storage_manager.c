#include "storage_manager.h"
#include "esp_log.h"

static const char *TAG = "STORAGE_MANAGER";

esp_err_t storage_manager_init(void)
{
    ESP_LOGI(TAG, "Storage manager initialized (placeholder)");
    return ESP_OK;
}